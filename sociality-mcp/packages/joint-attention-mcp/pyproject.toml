[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "joint-attention-mcp"
version = "0.4.5"
description = "MCP server for scene grounding and joint attention"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mcp[cli]>=1.26.0",
    "pydantic>=2.7.0",
    "social-core",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
    "ruff>=0.3.0",
]

[project.scripts]
joint-attention-mcp = "joint_attention_mcp.server:main"

[tool.hatch.build.targets.wheel]
packages = ["src/joint_attention_mcp"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
